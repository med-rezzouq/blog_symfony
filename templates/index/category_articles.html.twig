{% block category_articles %}
<div class="section">
    <div class="container">
        <div class="row g-5">
        
            <div class="col-lg-6">
                <div class="row mb-4">
                    <div class="col-12">
                        <h2 class="h4 fw-bold">Sports</h2>
                    </div>
                </div>
                <div class="row justify-content-center"> 
                	{% for article in home %}
                {% if article.category.name == 'sports' %}
              {{ include('partials/_card_article.html.twig' , {'parent_class' : 'col-lg-12' , 'child_class': 'me-md-3', 'article' : article}) }}
             {% endif %}
             {% endfor %}
           
                </div>
            </div>

  <div class="col-lg-6">
                <div class="row mb-4">
                    <div class="col-12">
                        <h2 class="h4 fw-bold">Business</h2>
                    </div>
                </div>
                <div class="row justify-content-center"> 
                	{% for article in home %}
                {% if article.category.name == 'business' %}
                {{ include('partials/_card_article.html.twig' , {'parent_class' : 'col-lg-12' , 'child_class': 'me-md-3', 'article' : article}) }}
             {% endif %}
             {% endfor %}
           
                </div>
            </div>

</div>

{% endblock %}